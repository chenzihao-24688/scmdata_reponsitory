CREATE OR REPLACE TRIGGER MRP."CLASS_ID_SEQ" BEFORE INSERT ON "MRP"."MRP_MATERIAL_CLASSIFICATION_ARCHIVES" REFERENCING OLD AS "OLD" NEW AS "NEW" FOR EACH ROW
begin 
	select class_id_seq.nextval into :new.ID from dual; 
end;
/

